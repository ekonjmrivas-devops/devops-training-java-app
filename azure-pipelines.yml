# azure-pipelines/010-pipeline-dummy.yml
# Pipeline dummy para validar Service Connection y permisos del agente

trigger: none

pool:
  vmImage: ubuntu-latest

steps:
  - script: |
      echo "=== Pipeline dummy Azure DevOps ==="
      echo "Rama:    $(Build.SourceBranchName)"
      echo "Commit:  $(Build.SourceVersion)"
      echo "Agente:  $(Agent.Name)"
      echo "Pool:    $(Agent.PoolName)"
    displayName: "Info del agente"